#include "esp_log.h"
#include "esp_err.h"

static const char *TAG = "MQTT";

esp_err_t mqtt_start(void) {
    ESP_LOGI(TAG, "mqtt_start (skeleton)");
    // TODO: Create and start mqtt_client using configs
    return ESP_OK;
}